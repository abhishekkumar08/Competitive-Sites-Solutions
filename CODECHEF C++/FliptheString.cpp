#include <bits/stdc++.h>
using namespace std;

void solve(){
    string a, b;
    cin >> a >> b;
    int maxa=0, count=0;
    for (int i = 0; i < a.length(); i+=2)
    {
        for (int j = 0; j < b.length(); j+=2)
        {
            
        }
        
        
        maxa=max(maxa,count);

    }
    
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    int t;
    cin >> t;
    while (t--)
    {
        solve();
    }
}