#include <bits/stdc++.h>
using namespace std;

void solve()
{
    long long r, c;
    cin >> r >> c;
    cout << r * c << endl;
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    int t;
    cin >> t;
    while (t--)
    {
        solve();
    }
}