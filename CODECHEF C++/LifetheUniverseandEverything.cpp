#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    while (1)
    {
        int a;
        cin >> a;
        if (a == 42)
            break;
        else
            cout << a << endl;
    }
}